<div class="page-header">
      <h1> <%= @property.name %> <small>£<%= @property.price %> pcm.</small> <a href="/contact-us" class="btn btn-success">Enquire Now</a>

</div>
<% if @property.pictures.any? %>
  <div class="container-fluid">
  <div class="row">
    <div class="col-sm-4 col-md-4">
      <strong><%= @property.description %></strong>
    </div>
    <div class="col-sm-8 col md-8">
  <% @property.pictures.each do |picture| %>
    <div class="col-sm-6 col-md-6">
    <div class="thumbnail">
  <%= image_tag picture.image(:large), class:"img-rounded" %>
</div>
</div>
  <% end %>
</div>

</div>
</div>
  <% end %>
